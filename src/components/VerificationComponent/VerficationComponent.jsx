export default function VerificationComponent({ children }) {
    return (
        // bg-white/95 da un blanco casi opaco, y backdrop-blur-sm hace un efecto cristal
        <div >
            {children}
        </div>
    );
}